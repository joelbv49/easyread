things to do for docs
1.create a doc space whenever a new conversation is created
2. create another handleClose to operate the messageBox
3. when clicked on summarize you have to call a function summarizePDF 
4. use useEffect to update the msg when the id,chat or request is changed






<div className="w-full h-full bg-white rounded-lg shadow-lg overflow-hidden">
      <div className="p-4  flex font-custom2 bg-slate-900 text-white  items-center border-b h-12 w-full">
        <h2 className="text-md font-semibold ">
          {fileType === 'website' ? 'Website Preview' : 'File Preview'}
        </h2>
      </div>
      <div className="w-full h-[calc(100%-4rem)]">
        {renderContent()}
      </div>
    </div>